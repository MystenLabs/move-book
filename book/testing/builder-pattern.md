# Pattern: Builder

## Examples of Builder Pattern

Currently, there is just one application of this pattern in the Sui Framework - the
`TxContextBuilder` type. It is used to build a `TxContext` for `Scenario`, and allows
customizing the context for a specific transaction.


